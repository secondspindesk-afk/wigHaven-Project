# Development Environment Config

# Local Backend (via Vite Proxy or Direct)
# Note: Vite proxies /api to http://localhost:5000 automatically
VITE_API_URL=http://localhost:5000

# Local WebSocket
VITE_WS_URL=ws://localhost:5000
